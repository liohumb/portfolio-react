import './home.scss'
import Hero from '../../containers/hero/Hero'

export default function Home(  ) {
    return (
        <main className="home">
            {/* HERO */}
            <Hero/>
        </main>
    )
}